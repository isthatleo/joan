# TODO

## Vercel Analytics + Speed Insights setup
- [ ] Install `@vercel/analytics` and `@vercel/speed-insights`
- [ ] Add `<Analytics />` and `<SpeedInsights />` to `app/layout.tsx`
- [ ] Run `npm run lint` and `npm run type-check`
- [ ] Run `npm run build` to confirm Next.js integration

